# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node (in case anyone runs commands locally)
node_modules/
npm-debug.log*

# Local test scratch
/tmp-test/

# Cross-session handoff briefs (ephemeral, never committed)
/.handoff/

# Dogfooding: superpowers-bridge 以「實體副本」裝在 openspec/schemas/ 底下。
# (junction / symlink 驗證得過 `openspec schema validate`,但 `openspec schemas`
#  掃不到,所以不能用連結。)
# 唯一來源是根目錄的 /superpowers-bridge —— 改完它要重新同步:
#   rm -rf openspec/schemas/superpowers-bridge && cp -R superpowers-bridge openspec/schemas/
/openspec/schemas/

# `openspec init/update --tools claude` 產生的 slash command 與 skill。
# 內容隨 OpenSpec CLI 版本漂移,不進版控;新 clone 跑一次即可重建:
#   openspec init --tools claude
/.claude/commands/opsx/
/.claude/skills/openspec-*/

# 跨 session 討論素材(大型對話匯出 / 階段二素材),不進版控
/Orca Worktree 模型分析.md
/2026-08-25-brainstorm-派工模式判準.md
/2026-08-26-監督式協調-攜出討論包.md

# auto-loop hook 的 session 狀態檔(純工具狀態)
/.claude_review_state.json
.workflow-harness/
