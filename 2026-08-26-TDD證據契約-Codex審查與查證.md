# TDD 證據契約 — Codex 第三方審查與我方查證

> 日期：2026-08-26
> 受審文件：`2026-08-26-TDD證據契約-設計結論-待第三方審.md`
> 審查者：Codex（gpt-5.2-codex，read-only sandbox，cwd = repo 根）
> 交叉驗證：Gemini **失敗**（見附錄 A）
> **結論：Codex 不接受該設計。我方逐條查證後，接受絕大部分指控。**

---

## 0. 先講結論

**核心問題仍然成立**：TDD 確實懸空、`schema.yaml` 第 507-513 行確實在叫 agent 放手。Codex 的事實查核第 1、2 條完全確認這點。

**但原設計有六處站不住**：

| 破口 | 影響 |
|---|---|
| 升 v2 **不隔離任何東西** | D6 的成本論證整個崩塌 |
| D7 的 blocking **不是真 gate** | 「不准 N/A」只是 prompt policy |
| evidence 進圖會**讓情況更糟** | 與 D6 的目的相反 |
| Requirement 標題**不是合格 key** | D3 無效（跨 capability 撞名）|
| **D1 與 D5 自相矛盾** | 宣稱確定性對照，實際仍需猜測 |
| commit SHA **不是 RED 證據** | D4 的證明力弱於宣稱 |

Codex 的收斂建議與使用者的 ROI 直覺一致：**把「修 TDD 漂移」和「建立可追溯性」拆成不同 change，兩者不該共用必要性論證。**

---

## 1. 我方親自查證的指控（附證據）

以下四條**不是轉述 Codex，是我方獨立重跑**。

### 1.1 ✅ 成立且致命：升 v2 不會隔離任何東西

**指控**：`version: 2` 不提供 change pinning，既有 v1 in-flight change 會立刻套用新 graph。

**查證**：讀 `dist/core/artifact-graph/types.js` 的 `ChangeMetadataSchema`：

```js
export const ChangeMetadataSchema = z.object({
    schema: z.string().min(1, ...),      // 只有 schema 名稱
    created: z.string().regex(...).optional(),  // 只有建立日期
});
```

**`.openspec.yaml` 沒有版本欄位。** change 只綁 schema **名稱**，永遠解析到該名稱的當前版本。

**推論**：`schema.yaml` 的 `version:` 欄位在執行期**什麼都不做**，純粹是標籤。原文件「升 v2 會影響既有採用者、所以要寫 migration note」的推理是錯的——migration note 隔離不了任何東西。

**Codex 的修法（我方認為正確）**：真要 major 隔離就得用**新的 schema 名稱**（如 `superpowers-bridge-v2`），或先改 CLI metadata。

### 1.2 ✅ 成立且比指控更糟：D7 的 blocking 不是真 gate

**指控**：官方 archive 對未完成 artifact 只 warning 並允許繼續。

**查證**：讀 `dist/core/archive.js`：

```js
const incompleteTasks = Math.max(progress.total - progress.completed, 0);
if (incompleteTasks > 0) {
    if (!options.yes) {          // ← 只有非 -y 才問
        const proceed = await confirm({ message: `Warning: ${incompleteTasks} incomplete task(s) found. Continue?` });
        ...
    }
}
```

**用 `-y` 時連問都不問，直接歸檔。** 而本 schema 的 apply 第 5 步指定的正是 `openspec archive -y`。

而且它**完全不檢查自訂 artifact graph**，只看 tasks 打勾數。

**推論**：D7 的「blocking FAIL」是純 prompt policy，引擎層沒有任何強制力。要真 gate 必須靠 CI 或 wrapper。

### 1.3 ✅ 成立：`post_apply` 不是「會報錯」，是「靜默通過但無效」

**指控**：`z.object()` 沒有 `.strict()`，未知欄位會 validate 成功然後被丟棄。

**查證（實測）**：在 `schema.yaml` 尾端加上

```yaml
post_apply:
  requires: [verify]
  instruction: |
    這是一個引擎不認得的欄位
```

跑 `openspec schema validate superpowers-bridge` → **`✓ Schema 'superpowers-bridge' is valid`**

**推論**：原文件說「沒有 post_apply」語意上正確但**表述不足**。真實情況更危險：**寫了會通過驗證、但完全沒作用**。任何人都可能加上它並以為生效。

### 1.4 ✅ 成立：這是我方的錯誤 — code-review 不是每張 task 一個

**指控**：`SKILL.md:223-229` 允許把同形小 task batch 成一次 dispatch、一次 diff review。

**查證**：讀 SDD `SKILL.md` 第 223-229 行：

> **Batch small same-shape work.** When the plan lists several tasks that are each a small, independent edit of the same kind ... **do not dispatch one subagent per task.** Compose ONE dispatch brief listing every file and its change, send the whole batch to a single subagent, and **review its diff as one unit.**

第 415-419 行另外允許 controller 在第 5 輪後 park 掉**它自己認定為真實**的 finding：

> **Real, but nothing downstream builds on it:** park it the same way, with a ruling that says it's real and deferred.

**推論**：我方在 2026-08-26 的 README 重新查證紀錄裡寫「每任務一 reviewer + 最終 reviewer，都寫在 SKILL.md 結構裡 ✅ 仍成立」——**這句話有條件沒講**。這正好犯了使用者全域規範裡「先讀『實際驗到什麼』再讀『名字說驗什麼』」那一條。

**已修正**：兩份 README 的該列已改為 ⚠️ 並附行號說明。

---

## 2. 我方認為 Codex 在本環境判斷錯誤的一條

### ❌ PRECHECK 的 POSIX 指令在 Windows 不可攜

**Codex 的指控**：`schema.yaml` 第 206-210、261、313-320 行使用 `$(...)`、`/dev/null`、`wc`、`grep`、`test -f`、`! grep`，這些不是 PowerShell 可依賴的命令，而本環境正是 Windows，PRECHECK 可能先壞。

**我方判斷：對其他採用者成立，對本機不成立。**

使用者全域 CLAUDE.md 明載：

> environment 自述「Shell: PowerShell」是誤導——`!` 前綴與 Bash 工具實際都走 `/usr/bin/bash`。

本次對話全程執行 POSIX 指令，無一失敗。

**保留價值**：這仍是**可攜性顧慮**，值得記入未來的採用者文件——但不是本環境的阻礙。

---

## 3. 我方接受但未逐一實測的指控

以下依 Codex 的論證強度接受，未獨立重跑。

### 3.1 evidence 進圖會讓情況變糟（不是變好）

引擎只查檔案存在（`state.js:11-29`），空檔也算存在（`outputs.js:15-37`）。因此 `evidence requires plan` 會讓 evidence 在 plan 完成後**立刻變 ready**，而 `/opsx:continue` 的行為就是「挑第一個 ready 的生出來」。

**這不是「有人惡意產空檔」的邊緣案例，是標準流程的預設行為。** 而若 evidence 的 instruction 寫 STOP，流程就會**永久卡在同一個 ready artifact**。

### 3.2 Requirement 標題會撞名

只抄標題沒有 capability namespace。兩份 spec 都有 `### Requirement: Error handling` 時，完全比對會**錯誤合併**——而不是 fail loud。原設計「完全比對失敗會大聲」的論證在此失效。

至少要用 `capability + requirement ID`。

### 3.3 D1 與 D5 自相矛盾（我方認為這條最重要）

> D1 說 Scenario 是 truth source，D5 卻拒絕 scenario-level mapping，verify 最後仍只能猜哪個測試涵蓋哪個 scenario，**正好回到你們批評的 keyword heuristic**。

而且 D5 的論證是**假兩難**。原文說「scenario 對照會逼測試名稱跟規格措辭綁死」，但可以用 ID 對應：

```
SCN-auth-expired-token → tests/auth_test.py::test_rejects_expired_token
```

**測試名稱不必等於 scenario 文案。** 我方當時未想到這層，用一個不存在的困境否決了可行方案。

### 3.4 commit SHA 不是 RED 證據

同一個最終 commit 裡同時有測試與實作，只能證明「現在綠」，不能證明「先紅後綠」。

延伸：reviewer 被明令**不要重跑 implementer 已跑過的測試**（`task-reviewer-prompt.md:64-89`），而 verify 在最終程式碼上重跑 GREEN 也無法重現「實作前曾看到 RED」。

**推論**：只有在證據由 CI/harness **自動捕獲**、綁定 commit SHA 且可重跑時，濃縮摘要才足夠。目前是同一代理鏈**手抄自述**，證明力不足。

### 3.5 上游自身的契約裂縫（新資訊）

`task-reviewer-prompt.md:75-76` **假設** implementer 已提供 TDD evidence，但 implementer prompt 只在 task 要求時提供。

這是 Superpowers **內部的**契約不一致，不是我方造成的——但它支持「TDD 目前處於未定義狀態」這個核心論點。

### 3.6 其他（依 Codex 排序）

- `tasks.md`（`X.Y`）、`plan.md`（`Task N`）、SDD（可 batch）**三層沒有共同身份**，`evidence.md` 的 `Task 3` 指哪一層未定義
- 新建的 Git worktree 可能**看不到未 commit 的 planning artifacts**，設計沒有 commit/staging precondition
- 嚴格禁止 N/A 會**誘發假證據**（文件、授權、純 refactor 未必有合理的新 RED）
- 單一 `evidence.md` **不適合未來 Orca 並行**（多 controller append 會衝突）
- Codex 亦指出 `MAX_DELTAS_PER_CHANGE` 是 **Zod `.max(10)` 硬上限**（`change.schema.js:22-24`），不只是建議

### 3.7 統計宣稱的可重現性

Codex 指出原文件第 7 節的三組統計（1,020 scenario / 65% 可斷言 / 中位數 18）**在 cwd 內無法重現**——沒有原始 spec、沒有正則、沒有腳本。

且有一處內部不一致：D3 寫「306 個中僅 1 個帶編號（0%）」，第 7 節寫「0% 帶編號」。精確值是 **1/306 ≈ 0.33%**。應標成「0%（1/306，四捨五入）」。

**我方接受**：統計是在別的 repo（`D:\workflow-harness`、`D:\專案資料夾\1150511googleWorkspace`）上跑的，未附腳本，確實不符合「可交叉驗證」標準。「65% 可直接寫成斷言」尤其站不住——正則只能證明**含有某些 token**，不能證明**語意可直接變成 assertion**。

---

## 4. Codex 的替代設計（原文摘要，供下輪參考）

1. **立即做一個不升 major 的 drift hotfix**：刪掉 `schema.yaml:507-513` 的假保證；apply dispatch 契約要求 behavior-changing task 附 RED/GREEN；缺失直接退回 implementer
2. **把 traceability 與 TDD 修復拆成不同 change**——前者是 provenance/governance 功能，後者只是修正錯誤的 transitive assumption，**兩者不應共用「必要性」論證**
3. **不用 Requirement 標題當 ID**：改用 `REQ:<capability>:<slug>` / `SCN:<requirement-id>:<slug>`，標題只供顯示
4. **證據改成每 task 一份機器可讀檔**（如 `evidence/task-003.json`），含 task ID、Requirement/Scenario IDs、RED command + exit code + 預期失敗原因、RED 時的 base SHA、GREEN command + exit code + HEAD SHA、test node IDs、reviewer verdict。原始 log 作 CI artifact，**摘要由工具生成，不讓 controller 手抄**
5. **沒有真正 post-apply phase 前，不把 evidence 假裝成 planning artifact**——它應是 apply sidecar output
6. **若一定要 v2，發布成新 schema 名稱**，保留 `superpowers-bridge` v1
7. **把 archive gate 放進可執行檢查**（CI/wrapper）；做不到就稱它為 policy，不要稱為 blocking boundary

---

## 5. 下一輪要重新決定的事

原設計的 D1-D7 中，**D2 以外全部需要重新檢視**：

| 決策 | 狀態 |
|---|---|
| D1 驗收真相來源 = Scenario | ⚠️ 與 D5 矛盾，需一併重定 |
| D2 鑰匙放 tasks.md、單向往上指 | ✅ 方向仍成立 |
| D3 鑰匙 = 原樣照抄標題 | ❌ 撞名、parser 不保存標題 → 需改 ID |
| D4 證據 = 濃縮事實、controller 手抄 | ❌ 證明力不足 + 手抄違反 context isolation |
| D5 Scenario 不獨立成行 | ❌ 論證是假兩難 |
| D6 升 v2、evidence 進圖 | ❌ 版本不隔離 + 進圖會變糟 |
| D7 blocking FAIL | ❌ 引擎層無強制力 |

**Roadmap 的 Change 1（修正錯誤宣稱）不受影響，可照原計畫進行。**

---

## 附錄 A：Gemini 交叉驗證失敗紀錄

**症狀**：`IneligibleTierError: This client is no longer supported for Gemini Code Assist for individuals.`

**已排除的可能**：

| 嘗試 | 結果 |
|---|---|
| 升級 CLI 0.55.1 → 0.57.0（最新） | 升級成功，問題不變 |
| 檢查環境變數 `GEMINI_API_KEY` / `GOOGLE_API_KEY` | 不存在 |
| 檢查 5 個專案目錄的 `.env` 與 `~/.claude` 設定 | 無 API key |
| 尋找 Antigravity CLI 可執行檔 | `~/.gemini/antigravity-cli/bin/` 底下只有 `webm_encoder.exe` |

**根因**：伺服器回傳 `tierId: 'free-tier'`、`reasonCode: 'UNSUPPORTED_CLIENT'`。認證方式為 `oauth-personal`，帳號 `kyoe33@gmail.com` 被判定為免費層。

**未查清**：使用者自述有訂閱，但訂閱類型未知。Google AI Pro/Ultra（消費者）與 Gemini Code Assist（開發者授權）是兩套不同的東西，CLI 的 oauth 路徑查的是後者。

**可行路徑（待使用者提供資訊）**：AI Studio API key 設 `GEMINI_API_KEY`；或 Code Assist 授權設 `GOOGLE_CLOUD_PROJECT`；或訂閱在另一個 Google 帳號（需互動式 `/auth` 切換）。

**影響**：本輪只有單一外部視角。Codex 在 Windows 可攜性那條的誤判顯示**它也有盲點**，第二視角仍有價值。
