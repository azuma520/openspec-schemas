# SDD ledger — plan: openspec/changes/fix-v2-blocking-defects/plan.md

Spec: openspec/changes/fix-v2-blocking-defects/specs/{plan-contract,tdd-evidence-contract,tdd-claim-accuracy}/spec.md (reachable — rulings are not provisional)
Worktree: .claude/worktrees/loosen-plan (branch worktree-loosen-plan)

## Preflight conflict scan (run before Task 1.1)

### Pairs sharing a file or an interface

| A | B | A produces / B consumes | Finding |
|---|---|---|---|
| 1.1 | 1.4 | f8,f9 dirs -> README rows | clean |
| 1.2 | 1.4 | f10,f11,f12 dirs -> README rows | clean |
| 1.3 | 1.4 | f13 dir -> README rows | clean |
| 1.1 | 2.1 | f8,f9 -> RED/GREEN subjects | ORDER-CRITICAL: RED must be run against pre-edit check 12 wording |
| 1.2 | 2.2 | f10,f11,f12 -> RED/GREEN subjects | ORDER-CRITICAL: same |
| 1.3 | 2.3 | f13 -> RED/GREEN subject | ORDER-CRITICAL: same |
| 2.1 | 2.2 | same file superpowers-bridge/schema.yaml | CONFLICT-RISK: shared file, must be strictly sequential |
| 2.1 | 2.3 | same file schema.yaml | same |
| 2.2 | 2.3 | same file schema.yaml | same |
| 2.2 | 2.4 | checks 9-11 grammar -> tasks instruction wording | clean (plan declares the dependency); also shares schema.yaml |
| 2.1,2.2,2.3 | 3.1 | check text -> templates/verify.md + tasks.md | clean |
| 2.1,2.2,2.3 | 3.2 | check text -> bridge README en+zh-TW | clean |
| 2.3 | 3.1 | freshness affected sets -> template footnote | clean (both must state: tasks.md -> checks 2,7,8-12; plan.md -> check 12 only) |
| 3.1,3.2 | 5.1 | superpowers-bridge/ final state -> dogfood copy | ORDER-CRITICAL: 5.1 must run after every superpowers-bridge/ edit |
| 2.1,2.2,2.3 | tasks.md of this change | each writes its own RED/GREEN records into the same file | CONFLICT-RISK: shared file, sequential only |
| 4.1 | 5.1 | canonical spec vs dogfood copy | clean (different trees) |
| 4.2 | 3.2 | CLAUDE.md coupling row names the README Compatibility v2 row | clean, but both must leave the v2 row byte-identical |

### Per-task self-consistency

| Task | Does its own text agree with itself? |
|---|---|
| 1.1 | yes — f8/f9 break exactly one thing each; PASS under pre-edit wording is the property 2.1 needs |
| 1.2 | yes — f10/f11 negative, f12 positive control |
| 1.3 | yes — plan.md with no task rows is exactly what v2 mandates |
| 1.4 | yes — bidirectional row/dir check is stated |
| 2.1 | yes — branch order (non-numeric, absent plan.md, duplicates, set compare) is fully ordered |
| 2.2 | yes |
| 2.3 | yes — "every reference, not one" plus a grep control |
| 2.4 | yes — residual-wording grep is the stated control |
| 3.1 | yes |
| 3.2 | yes — Compatibility table shape explicitly frozen |
| 4.1 | yes — grep must carry the markdown backticks (prior silent 0-hit recorded) |
| 4.2 | yes — scope fenced to named sentences |
| 5.1 | yes |
| 5.2 | yes — append-only |
| 5.3 | yes — stop-and-reopen on a changed premise |

### Rulings from the scan

Ruling: execute 2.1 -> 2.2 -> 2.3 -> 2.4 strictly sequentially, never batched or parallel — four tasks edit one schema.yaml and three also append records to one tasks.md. Cost if wrong: none (order is a superset of the plan's blocked_by graph); the alternative risks lost edits.
Ruling: group 1 (all four fixture tasks) lands and is committed before ANY schema.yaml edit, so every RED in 2.1-2.3 is obtained against pre-edit wording as tasks.md boundary 1 requires. Cost if wrong: the RED records become retro-written claims, which is exactly the defect this change exists to close.
Ruling: for f12 the RED's ACTUAL verdict is recorded as "indeterminate — the pre-edit check 11 text does not decide which RED pairs with which GREEN", not as a BLOCK/PASS token. The plan itself says "indeterminate or wrong". Cost if wrong: a reviewer may read the RED as unobtained; mitigated by stating the pre-edit sentence quoted verbatim beside it.
Ruling: reviewers are told in the constraints block that mutation fixtures are DELIBERATELY broken artifacts — near-duplicate content between fixture dirs and single-defect files are the design, not review findings. Cost if wrong: none; it is a factual scope statement, not a "do not flag" instruction (the reviewer may still flag a fixture that breaks more than one thing).
Ruling: 5.1 runs after 3.1 and 3.2, not merely after group 2 — the dogfood copy takes the whole superpowers-bridge/ tree including templates and READMEs. Plan 3.1/3.2 Interfaces already say this. Cost if wrong: a stale dogfood copy would render old template text at verify.

Ruling: subagents MUST NOT run git add/commit/push. rules/git-workflow.md Anchor Register #4 forbids it outside the enumerated workflows, and a subagent is Claude. SDD's "implementer commits" step is therefore carried by the controller running /smart-commit --execute (user-approved) after each task group. Cost if wrong: review packages are built from working-tree diffs instead of commit ranges until the group commit lands; no loss of review coverage. Established precedent: the 2026-09-07 sessions committed this change's artifacts the same way.
Ruling: scripts/task-brief could not extract entries (it matches '## Task N'; the opsx Plan Contract uses '## N — title'). Briefs are generated by an equivalent extractor writing the same per-task brief files. Cost if wrong: none — the brief content is the verbatim plan entry plus the tasks.md line, header and global constraints.

BASE (before task 1.1-1.3): 6c4605e

## Execution

Tasks 1.1-1.3: batched into one implementer dispatch (same shape: author a self-contained fixture pair). Reported DONE; 12 new files, 6 directories, nothing tracked modified. Report: task-1.1-1.3-report.md. Implementer concern carried forward: check 9's "must include one RED/GREEN line" is ambiguous between presence and exactly-one; it read presence-only. That reading is what makes f9/f11 isolable, and 2.2 must settle it explicitly.
Task 1.1-1.3: review dispatched (opus) with package review-pkg-1.1-1.3.md.
Task 1.1-1.3: review 1 -> Needs fixes. 0 Critical, 1 Important (f11 second RED carries a harness-error-shaped failure: excerpt -> candidate R1 violation -> fixture would carry two findings, losing isolation), 2 Minor. Full report: review-1.1-1.3.md.
Task 1.1-1.3: minor (deferred): report's Concerns paragraph names f9 as hinging on check 9's cardinality reading; f9 has exactly one RED and one GREEN and its duplicate is a plan key check 9 never reads. The pair that actually hinges on it is f11 and f12. Controller carries the corrected fact into the 2.2 dispatch instead of reopening the loop for a Minor.
Task 1.1-1.3: minor (deferred): f13/tasks.md is 4 lines vs f1-f7's 8-20; reviewer judged it exactly sufficient, and suggests one extra conforming task would make check 12 non-trivial for it too. Not fixed.
Task 1.1-1.3: fix round 1/5 dispatched (resumed original implementer) with the single Important finding.
Task 1.1-1.3: fix round 1/5 (1 addressed, 0 open — f11 second RED now FAIL + assertion-mismatch excerpt; no new breakage). Re-reviewer re-derived the isolation check by check.
Task 1.1-1.3: complete (working tree, no commit yet — controller commits the group via /smart-commit; review clean, 2 minors deferred above).
Task 1.4: implementer dispatched in parallel with the r1 re-review (different files; only implementers are serialized against each other, and 1.1-1.3's implementer was idle).
Task 1.4: review -> Approved, 0 Critical/Important. Reviewer independently re-derived the 13<->13 bidirectional match and confirmed no existing table row was altered (the 4 deleted lines are the sanctioned f6/f7 sentence and the two refreshed "how to re-run" sentences).
Task 1.4: minor (deferred, MUST be carried into group 2): the new f8-f11 rows cite specific check numbers (check 12, check 11). Group 2 has not landed yet; if 2.1/2.2 renumber or split a check (e.g. the subject grammar becoming its own check), those four rows need a follow-up correction. Group 2's dispatch carries this obligation.
Task 1.4: complete (working tree; review clean).
Group 1: committed 22c15cf "docs(poc): add mutation fixtures f8-f13 for the five v2 checker defects" (13 files; verify-last exit 0). Only the three deliberately-excluded handoff files remain untracked.

BASE (before task 2.1): 22c15cf
Ruling: tasks 2.1-2.4 run one implementer at a time on opus, not batched. They share schema.yaml, three of them append RED/GREEN records to the same tasks.md, and each is normative-prose surgery whose defect mode (a name claiming more than the text asserts) is exactly what this change exists to close. Cost if wrong: more dispatches than a batch would need.
Task 2.1: implementer reported DONE_WITH_CONCERNS. Concern 2 is a real scope gap, ruled on before review:
Ruling: schema.yaml:285 (the `plan` artifact instruction) defines "1:1" as "the set of entry keys and the set of task numbers must be identical in both directions" — the exact conflation D1 repudiates, and it also asserts "verify's deterministic key-set check reads exactly this form". No task in this change owns it. It is folded into 2.1 rather than deferred, because (a) the binding spec sentence defines the correspondence in two stages, so after 2.1 lands this sentence contradicts the spec; (b) it is not one of the six enumerated P2s, so the "no P2 pulled in" non-goal is not engaged — it is an inconsistency 2.1's own edit creates; (c) leaving it ships a surface telling authors 1:1 means set identity while the checker blocks duplicates. Scope is one sentence, in the file 2.1 already owns. Cost if wrong: a reviewer reads it as scope creep, which the ledger and the reviewer's constraints block answer.
Task 2.1: concern 3 noted (this change's own tasks.md 2.1 now carries two subjects under one task, which current check 11 cannot pair; legitimized when 2.2 lands). No action — the verify artifact is written after all of group 2 by construction.
Task 2.1: review 1 -> Needs fixes. 0 Critical, 1 Important (stage-one BLOCK's effect on stage two is undecided; :562 reads short-circuit, :583 reads both-report, and the GREEN record asserts the short-circuit as observed behaviour the instruction never specifies), plus a provenance defect the controller confirmed as a real gap. Full report: review-2.1.md.
Ruling: stage one does NOT short-circuit — both stages are evaluated and each reports its own findings. The binding spec sentence says a duplicate is reported "distinctly from a missing or extra key, because the two are different defects with different repairs"; a short-circuit hides the second defect behind the first, so an author fixes the duplicate, re-runs, and only then learns the sets also differ. Stage two on duplicated input is safe (reducing to sets cannot invent a difference), so evaluating both costs nothing. The instruction must say this in one clause rather than leave it to the executor. Cost if wrong: a verify run reports two findings where a maintainer expected one — visible, harmless, and reversible in one sentence.
Ruling: the RED record's baseline must name the commit a later reader can actually re-run (22c15cf, which contains the fixtures), not 5aa19bf. Evidence that cites an unreproducible baseline is the same class of defect this change exists to close. Cost if wrong: none.
Task 2.1: fix round 1/5 dispatched (resumed original implementer) with the Important finding, the confirmed provenance gap, and both rulings.
Task 2.1: minor (deferred, with ruling): the implementer's re-walk surfaced that the only input distinguishing short-circuit from non-short-circuit is one carrying BOTH a duplicate and a set difference, and no fixture has that shape (candidate f14). Ruling: NOT added in this change. Adding a fixture is not the cost — adding it as evidence would require a new task in tasks.md AND a matching entry in plan.md, because this change's own check 12 demands the two key sets be equal, so a one-fixture addition is a structural edit to the change's artifacts mid-implementation. The clause's two verdicts are both safe (stage two on duplicated input cannot invent a difference), and stage one is already evidenced by f8 and f9; the "both stages report" clause is a composition of two independently-evidenced stages. Surfaced to the user at the end as a follow-up candidate. Cost if wrong: the newly-worded clause ships without a case of its own.
Task 2.1: fix round 1/5 (2 addressed, 0 open — no-short-circuit clause at schema.yaml:568-570 with the terminal enumeration agreeing at :596-601; both RED baselines now cite 22c15cf). Re-reviewer diffed pre-edit against current and confirmed the edit is purely additive: every pre-existing branch and the stage-two message text survive verbatim.
Task 2.1: complete (working tree, uncommitted; review clean, 1 minor deferred with ruling).
Task 2.2: dispatched (opus). Pre-edit baseline for checks 9-11 is still 22c15cf — task 2.1 did not touch them.
Task 2.2: implementer hit a session usage limit immediately after writing its report. Controller verified the tree rather than the message: schema.yaml and tasks.md both carry the edits, task-2.2-red-walk.md was written at 17:04 (before the 17:09 edits, so the RED really does precede the edit), and task-2.2-report.md is complete through all 9 sections including concerns. No re-dispatch needed; the implementer is unavailable, so any fix round starts a fresh implementer (SDD rounds 4-5 rule applies early, by circumstance rather than by escalation).
Task 2.2: concern carried forward - review judgement R2 ("Checks 9-11 read the subject as an opaque string and compare it to itself") now under-describes: check 9 validates the subject's format and nothing is compared "to itself" any more. Under-claims rather than over-claims, so not a correctness defect. Outside 2.2's fence; routed to task 2.4's sweep.
Task 2.2: concern carried forward - f2-missing-green now additionally trips check 11 stage two alongside its check 9 BLOCK. The fixtures README row's verdict is unchanged, so no README edit is owed, but a re-runner reporting both checks is not wrong.
Task 2.2: review dispatched (opus).
Task 2.2: review -> Spec compliant, Approved with fixes. Reviewer independently walked f10/f11/f12 + f2/f3/f4 and confirmed every claimed verdict including f12 producing NO finding (the positive control survives); confirmed the RED is genuine against independently recovered pre-edit text; confirmed check 7, check 12 and the `tasks` instruction are byte-identical to 22c15cf. 3 Important, 3 Minor.
Task 2.2: the ledgered renumbering obligation from task 1.4 FIRED — fixtures README:45 still cites "check 11" for f10's subject-grammar row, which is now check 9. That table is the blind re-run answer key, so a correct run would grade as wrong.
Ruling: the R2 amendment is folded into this fix round rather than left to 2.4. The reviewer established that 2.4's residual-wording grep list does not match R2's phrasing, so routing it to 2.4 would mean nothing catches it — the routing I recorded earlier was made without that fact. R2 is in schema.yaml, which this round already has open. Cost if wrong: one extra sentence in 2.2's diff that a reviewer could have attributed to 2.4.
Ruling: two of the three Minors are fixed in this round under the one-line-fix-in-an-open-file exception (check 9's title dropping "present"; the required-field conformance sentence needing to be scoped to `subject:` so it does not imply a grammar for `outcome:` and `failure:` — that one is an overclaim, the exact defect class this change closes). The third Minor is deferred: check 11's collection being silent on a record with no `subject:` field is already decided upstream, because check 9 requires `subject:` and BLOCKs its absence.
Task 2.2: fix round 1/5 dispatched to a FRESH implementer (original hit a session usage limit and is unavailable), opus, carrying brief + report + findings.
Task 2.2: fix round 1/5 (5 addressed, 0 open; no new breakage). Re-reviewer re-derived all 13 README rows independently and confirmed only f10 was stale; confirmed R2 is still a bare judgement with no BLOCK condition added; confirmed the f11 RED holds under BOTH readings of the ambiguous pre-edit sentence by walking each; ruled the implementer's added sentence "No other field has a grammar here." warranted.
Task 2.2: complete (working tree, uncommitted; review clean, 1 minor deferred with ruling).
Task 2.3: dispatched (opus). Pre-edit baseline for check 7 is still 22c15cf — tasks 2.1 and 2.2 did not touch it (the 2.2 reviewer verified check 7 byte-identical to 22c15cf).
Task 2.3: implementer DONE, two judgement calls raised. Controller read the actual text rather than the summary and ruled BOTH acceptable, no fix round:
Ruling: check 7's absent-tasks.md branch reports "tasks.md absent — deferral state undetermined" and explicitly forbids recording it as "no deferred tasks", but does not BLOCK. This is asymmetric with check 12, which BLOCKs on an absent plan.md — and the asymmetry is principled: check 12 is the only check that reads plan.md, so if it did not block nothing would, whereas an absent tasks.md is blocked by checks 2 and 8-12 which all read it. The defect that mattered — claiming "no deferred tasks" when the check could not look — is closed. Cost if wrong: a missing tasks.md yields a non-blocking undetermined result from check 7 while other checks block anyway.
Ruling: check 7 keeps its own local DEFERRED TASK definition instead of joining the "Shared definitions, used by checks 8-11" block. The local definition is a strict subset of the shared TASK LINE definition and does not contradict it, and it adds two things the shared one lacks (only leading `- [~]` counts; the check reads tasks.md and nothing else). The shared block sits under a "CHECKS 8-12" heading, so widening its stated audience to 7-11 without moving it would create a worse inconsistency than the small duplication it removes. Cost if wrong: two places state the marker shape and could drift.
Task 2.3: review dispatched (opus), with both rulings named so the reviewer tests them rather than assuming them.
Task 2.3: review -> Needs fixes. 1 Important, 3 Minor. All four carrier references moved; the three procedures survive as diff CONTEXT lines (verified, not claimed); freshness map matches; no fifth reference; no D4 violation; RED genuine (red-walk mtime 08:14:31 < schema.yaml 08:15:28, and the reviewer re-walked both texts itself).
CORRECTION to an earlier ruling: Ruling A's PREMISE was false and the reviewer proved it. I wrote that an absent tasks.md "is already blocked by checks 2 and 8-12 which all read it". In fact checks 2 and 8-11 pass VACUOUSLY on an absent tasks.md — they are universally quantified over task lines and there are none. Only check 12 blocks, plus the verify PRECHECK's grep. The ruling's CONCLUSION (check 7 reports rather than blocks) survives; the premise does not — and the implementer had written that premise into the shipped text. I ruled on a premise I did not verify, which is the same defect class this change exists to close. Ruling B (local definition) was tested and holds: no line the two definitions classify differently.
Ruling: the fix names check 12 and the verify PRECHECK explicitly rather than gesturing at "the checks that read tasks.md". Cost if wrong: none — it replaces a false general claim with a true specific one.
Task 2.3: fix round 1/5 dispatched (resumed original implementer).
Task 2.3: fix round 1/5 (3 addressed, 0 open; no new breakage). Re-reviewer re-verified the REPLACEMENT premise the same way the original was falsified — read check 12's body and the verify PRECHECK itself and confirmed both really do catch an absent tasks.md, and that check 2 and checks 8-11 really are vacuously satisfied.
Task 2.3: complete (working tree, uncommitted; review clean).
Task 2.3: deferred (belongs to 3.1, already on its list): templates/verify.md still says plan.md for the §7 deferred-equivalence section.
Task 2.4: dispatched (opus). The `tasks` artifact instruction is still byte-identical to 22c15cf — 2.1 touched the `plan` instruction, 2.2 touched checks 9-11 and R2, 2.3 touched check 7 and the freshness map. None touched it.
Task 2.4: review -> Spec compliant, Approved. 0 Critical, 0 Important, 3 Minor. Reviewer independently re-ran all three greps plus a broader singular sweep, walked the four properties in both directions, walked the extended SHAPE example line by line against checks 8-11, and ruled both flagged judgement calls: the `a:::b` fix is right (check 9 names it conforming at schema.yaml:594-596) and the SHAPE "indentation exactly" asymmetry is a legitimate prescription the checks' own INDENT RULE reconciles by name.
Ruling: all three Minors are fixed on the spot rather than deferred, under the one-line-fix-in-an-open-file exception. Two of them are the agreement failure this task exists to close, just small: :196-199 only implies check 9's AT LEAST ONE, and :210-211 says "identical, character for character" while check 11 trims — making the instruction STRICTER than the checker, so a human reviewer would reject work the machine accepts. Cost if wrong: three extra lines in 2.4's diff.
Task 2.4: fix round 1/5 dispatched (resumed original implementer).
Note: the brief's residual-wording grep list was partly wrong — `the RED record's` never existed in the file. The implementer recorded that rather than silently dropping it, and the reviewer was told to assume the list might also be incomplete (it swept beyond it and found six correct singular hits, no incorrect ones).
Task 2.4: fix round 1/5 (3 addressed, 0 open; no new breakage). Re-reviewer compared each mirrored clause word by word against the check sentence it claims to mirror, and confirmed the indent guidance's SUBSTANCE is unchanged (only the overclaiming lead-in moved).
Task 2.4: complete (working tree, uncommitted; review clean).
GROUP 2 COMPLETE (2.1, 2.2, 2.3, 2.4). All four reviewed and re-reviewed; every finding addressed.
Group 2: committed cffe99a "fix(schema): make the v2 verify checks assert what their names claim" (3 files, +348/-77; verify-last exit 0).

BASE (before group 3): cffe99a
Task 3.1: review -> Spec compliant, Approved. 0 Critical, 0 Important, 5 Minor. Reviewer re-derived the check-list comparison itself (8-12 match 5/5 character-for-character after whitespace normalisation, all three renames included; old-title grep a superset of the implementer's returns zero), verified the freshness partition, and walked the tasks shape comment as an author.
Task 3.1: the render-evidence substitute was ruled SOUND. The reviewer verified the premise rather than trusting it: the installed copy at openspec/schemas/superpowers-bridge/ really is pre-group-2 (its verify.md still holds 2 old-title hits), so `openspec instructions` here would have rendered stale text. Refusing a check that cannot fail for the reason it claims to test was correct behaviour, not a gap.
CORRECTION to controller framing: I told the reviewer verify.md grew +406 lines. That figure is a LINE-ENDING artifact. With --ignore-cr-at-eol the real change is +62/-18 on verify.md and +27/-3 on tasks.md, ~+44 net. Every diffstat I have quoted in this run is inflated the same way, because the repo pins LF while the working copy is CRLF. Use --ignore-cr-at-eol when the size of a diff is part of the judgement.
Ruling: two of the five Minors are fixed on the spot (one-line, file already open) because both are the template-vs-schema agreement class this task exists to close: tasks.md:22 states the shape comment more narrowly than the schema does, and tasks.md:36-38 omits the optional `invocation:` field the schema documents. The three pre-existing ones (verify.md:54 house-style heading drift, verify.md:148 task-line enumeration, and the §4 residual the reviewer ruled rightly left) are deferred.
Task 3.1: fix round 1/5 (2 addressed, 0 open; no new breakage). Re-reviewer verified the widened sentence does not overshoot the schema, and tested the implementer's call to keep `invocation:` out of the example block by reading the schema's own SHAPE block (which likewise has no invocation line) — call ruled right. Three-subject counterfactual walked against checks 9-11 independently.
Task 3.1: complete (working tree, uncommitted; review clean, 3 pre-existing minors deferred).
Note: the implementer correctly pushed back on one of my corrections — its report never quoted the inflated diffstat figures; the line counts in it were render output sizes. The CRLF inflation is real and applies to MY packages, not to its report.
Task 3.2: dispatched (opus).
Task 3.2: fix round 1/5 (1 addressed, 0 open; no new breakage). Re-reviewer verified the Compatibility v2 row byte-identical by three independent methods after the second edit to that file.
Task 3.2: complete (working tree, uncommitted; review clean).
GROUP 3 COMPLETE (3.1, 3.2).
Ruling: tasks 4.1 and 4.2 are batched into ONE implementer dispatch. They are small, prose-only, single-file each, share no file and have no dependency between them — the SDD batching rule's exact case. Cost if wrong: one review covers two files instead of one each.
Controller check before dispatch (so the implementer is not guessing): .github/workflows/version-check.yml:44 greps `^\| v2 \| ` and README.md:520 is `| v2 | ...`, so the workflow and the README already agree; it is CLAUDE.md's coupling row that still says v1.
Tasks 4.1+4.2 (batched): review -> Spec compliant, Approved. 0 Critical, 0 Important, 3 Minor. Reviewer independently re-classified all four grep hits in the spec and confirmed both directions: :22 and :60 were assertions (:22 sat inside an "are conforming" carve-out, positively licensing the old carrier), :42 and :52 quote the wording only to forbid it and were rightly left. It also compared both edited requirement bodies PROGRAMMATICALLY against the delta spec and found them IDENTICAL, so the archive merge is idempotent.
Ruling upheld by review: CLAUDE.md:187 is in scope (the plan entry says "any schema-major sentence", not section-bounded), and minimal-and-flagged for `需從 2 bump` was right — a version-independent rewording would have been a fourth unauthorised correction failing the containment acceptance.
Ruling: one Minor fixed on the spot — the 4.1 control record in tasks.md:133 names two greps but calls :42/:52 "the other two hits", while :52 came from a third grep the record never names. The record under-describes its own method, so a later reader re-running only the named greps cannot reproduce the classification. Same defect class as the change itself, one level down. Cost if wrong: one extra clause.
FOLLOW-UP for the user at close-out: CLAUDE.md:187's `需從 2 bump` hard-codes today's schema major and will go stale at the next bump. Deliberately not fixed here (containment).
FOLLOW-UP, fenced: CLAUDE.md:87 「這個 repo 正在從 v1 往下一代改」 is still pre-v2 prose; it belongs to the separately registered governance-language task, not this change.
Tasks 4.1+4.2: minor fix verified by the CONTROLLER rather than a fresh re-review dispatch, because the fix was one clause in an evidence record and the check is decisive and cheap: I re-ran the record's three named greps against cffe99a myself. Union = {22,42,52,60}, exactly the four hits the record classifies; before the fix the named greps reproduced only {22,42,60}. Recorded here because a one-clause record repair with a mechanical reproduction check does not need a dispatched seat, and pretending otherwise would be process theatre.
Tasks 4.1+4.2: complete (working tree, uncommitted; review clean).
GROUP 4 COMPLETE.
Groups 3+4: committed 787b14c "docs(bridge): bring every coupled surface in line with the corrected checks" (7 files; verify-last exit 0).

BASE (before group 5): 787b14c
Task 5.1: MILESTONE - the edited schema.yaml passed `openspec schema validate superpowers-bridge` for the first time. Every earlier task could only run a YAML parse and each said so; this is the check that could have failed for the reason it claims to test.
DEVIATION (task 5.1): the `rm -rf openspec/schemas/superpowers-bridge` that CLAUDE.md prescribes was DENIED by the permission system, twice. The implementer did not spell a different delete to get past it — correct behaviour — and instead proved the end state equivalent: the copy held no file absent from the source, so an in-place `cp -R` reaches the same state. Controller verified independently: `diff -r` between source and copy is empty, `Only in openspec/...` is empty, and the old check title has 0 hits in the installed copy. Accepted. NOTE the equivalence held only because this change deleted no file from the bundle; where a future change removes one, cp-without-rm would leave it behind and the two are NOT interchangeable.

## Final whole-branch review

Verdict: Needs revision. 0 Critical, 5 Important, 5 Minor. Confirmed: all five P1 defects genuinely closed; the change PASSES ITS OWN checks 8-12; version-check.yml grep+awk re-run green; dogfood copy identical; D5 precondition still holds (no tags, 24 ahead); every RED citation to 22c15cf re-ran and resolved; twelve of thirteen fixtures still isolate under the NEW checks, and the `::` grammar did not retroactively break f1-f7 (the biggest risk in the change).

The five Important findings share ONE shape, and it is the same shape as the original five P1s: a scoped grep or a scoped task list was mistaken for a complete sweep. 2.4's residual-wording grep covered schema.yaml and stopped, so the phrasing survived in two READMEs. 3.1's delivered set named two templates, so the third (templates/plan.md) was never audited. The fixture answer key was written against each fixture's INTENT rather than re-derived under the NEW checks, so f2's row went stale. In each case the check that would have caught it was one command wider than the one that ran.
Ruling: the fix dispatch must end with a cross-surface sweep over EVERY bridge-owned surface for the load-bearing claims, not another per-file check. Adding that sweep is the finding's actual repair; fixing the five instances without it leaves the same gap for the next change. Cost if wrong: one extra grep pass.
Ruling: of the five Minors, only schema.yaml:634-637 is fixed (check 11's collection step is undefined for a record with no `subject:` field — gate-safe because check 9 blocks first over the identical scope, but the findings text is undefined, which is an undecided branch in a deterministic check). Deferred: verify.md:148 and :54 (pre-existing), CLAUDE.md:187 (follow-up for the user). tasks.md:61 — task 2.1's RED has no separate pre-edit walk file — is deferred and MUST NOT be "fixed": writing one now would be manufacturing a pre-edit artifact after the edit, which is the exact defect this change exists to close. Its 22c15cf citation is the stronger evidence anyway.
Final fix wave: the first fixer hit a session usage limit. Its last message read "I'll start by reading..." — but the TREE showed otherwise, so the controller inspected the diffs rather than believing the message (same lesson as task 2.2). Confirmed already done and correct: I1 (templates/plan.md two-stage wording), I2 (both READMEs, at-least-one + one pair per subject, saying the same thing in both languages), I3 (tasks 1.1-1.4 ticked, 5.3 correctly still unticked), M1 (check 11's no-subject clause). NOT done: I4 (fixtures README f2 row + the thirteen-row re-derivation), I5 (errata E3 correction), the cross-surface sweep, the dogfood re-sync now that schema.yaml changed again, and the report. Continuation dispatched on sonnet.
Final fix wave: re-review -> ALL SIX findings (I1-I5, M1) ADDRESSED, no new breakage. The re-reviewer spot-checked four fixture rows including both positive controls (f7, f12), re-checked two sweep claims across every surface itself rather than accepting the report, verified the errata correction is genuinely append-only AND factually right (it read pre-edit check 11 at 22c15cf and confirmed indeterminate, not ordinal), and re-ran the dogfood diff and both openspec commands.
SDD IMPLEMENTATION COMPLETE. All 15 plan entries delivered; 14 ticked, 5.3 deliberately held for immediately before the verify artifact.
Next: external doc-plane gate (Codex) -> 5.3 -> verify artifact -> retrospective -> archive -> push/PR/CI.

## External doc-plane gate (Codex, not fallback)

Target set: all 22 .md files this change touched (6c4605e..working tree), handoffs excluded. NOT the default `git diff HEAD` set — three of the four commits are already committed, so the mechanical default would have reviewed only the last fix wave and called the change reviewed. The honest scope is the whole change.
Tier: ESCALATED to `thorough` from the unset baseline (`standard`). Reason: superpowers-bridge is a published contract other repos install and execute — the auto-loop's `thorough` row names public API, and a wrong sentence here propagates to every adopter. Stated per rules/auto-loop.md § Tiers.
Deterministic link check: 10 checked, 0 failures, 0 unresolved — passed to Codex as established findings so it does not spend a pass re-deriving them.
Batches: 2 (12 files / 37KB, 10 files / 164KB). Both dispatched to Codex proper; both moved to background.
Doc gate batch 0 (Codex): ⛔ Needs revision. 2 red, 1 yellow. Controller verified BOTH reds against the repo before ruling (an earlier ruling this run was overturned for resting on an unverified premise):
- CLAUDE.md:24 — CONFIRMED. The structure tree says 「本 repo 無 openspec/,不跑自己的 bridge 流程」 while the dogfooding section at :152 says the opposite, and openspec/changes/fix-v2-blocking-defects/ demonstrably exists. A genuine internal contradiction that hands the next agent mutually exclusive workflow guidance. Will fix — one parenthetical, in a file already in this change's baseline.
- CLAUDE.md:111 — FALSE POSITIVE, caused by the reviewer running inside a git worktree. All three named discussion files DO exist in the repo's main working directory (verified by listing it); they are absent HERE because they are untracked and a worktree does not receive untracked files. CLAUDE.md already states 「三份都未進版控」. Dismissed on evidence, not on judgement. A half-clause noting a clone or worktree will not have them is worth adding anyway, because this session is the exact case that tripped over it.
[DISMISS_VERDICT] CLAUDE.md:111 | reviewer claimed three named files do not exist in the repo root | NON_ACTIONABLE: they exist in the main working directory and are untracked by design; the reviewer was in a worktree | evidence: directory listing of C:/Users/user/orca/openspec-schemas/
Doc gate batch 1 (Codex): ✅ Mergeable. 5/5/5 on the applicable dimensions, zero findings at any severity. This is the batch carrying the change's core — both bridge READMEs, all three templates, the canonical spec, the change's own tasks.md and the f9 fixture — and Codex independently confirmed the claims match schema.yaml, the installed bundle matches the source, and checks 8-12 render with the corrected semantics.
Doc gate plan verdict: BLOCKED on batch 0 only. Per the skill, the conjunction is behaviour-layer and the state slot cannot hold it: the plan is Mergeable only when the LATEST dispatch of EVERY batch passed. Batch 1 passing does not close batch 0.
Fix wave for batch 0: CLAUDE.md:24's contradiction (confirmed) + the fixtures README f13 row's missing terminal verdict (yellow, but it is the answer key again — the same surface that already produced one Important finding this run). CLAUDE.md:111 dismissed on evidence; a half-clause is added anyway because a worktree really cannot see those files and this session is the case that proved it.
Doc gate batch 0, round 2 (Codex reply on the same thread): CLAUDE.md:24 ADDRESSED; CLAUDE.md:111 ADDRESSED and the ORIGINAL FINDING WITHDRAWN by the reviewer (it verified the three files exist in the main working directory and even confirmed the 43k-line claim at 43,656 lines) — the dismissal held on evidence. f13 row NOT ADDRESSED, with a new red: the amended row MISSTATED the old behaviour (old check 7 required plan.md to carry `[~]` rows, and f13's plan.md has none, so it did not block either), and "no BLOCK" is the same answer under both wordings, so the binary question cannot score the fixture at all.
CONTROLLER ERROR: I re-dispatched the f13 fix to a fresh agent while the original agent still had the instruction unprocessed in its inbox, because its idle notification showed stale content and I read that as "did not receive it". Two implementers on one file. Caught before any conflicting write; the second was stopped and asked to report any edit and its independent derivation. The rule I broke is the SDD one I had been keeping all run: never dispatch two implementers in parallel. The trigger was a stale idle notification — the same class of stale-message misreading that this run has already caught twice in the other direction (an agent reporting "not started" when the tree showed the work done). Check the tree, then the inbox state — not the notification text.
f13 fix (by the original agent, verified in place): the row now records the discriminating result — current check 7 reads tasks.md and finds exactly 1 deferred task; old check 7 reads plan.md and finds 0 — and states plainly that NEITHER blocks, with each BLOCK condition quoted. Re-run step 3 now asks for check 7's deferred-task count and identity in addition to BLOCK/no-BLOCK, for that check only.
Doc gate batch 0, round 3 (Codex): ✅ Mergeable. It re-derived both check-7 behaviours a third time against the fixture and confirmed the row matches, ruled the pair scorable, ruled the protocol exception coherent for the other twelve, found no new problem, and confirmed nothing from its original report remains outstanding.
DOC GATE PLAN VERDICT: ✅ Mergeable — the LATEST dispatch of BOTH batches passed (batch 1 first time with zero findings; batch 0 on round 3). Noted via review-state.js note doc_review pass.
Independent-review note for the record: this external gate found two real defects that eleven internal review seats had all passed — CLAUDE.md's structure tree contradicting its own dogfooding section, and an answer-key row that could not score the fixture it documents. It also produced one false positive, caused by running inside a git worktree, which the controller dismissed on evidence and the reviewer then withdrew itself. That is the降級 clause working in both directions: the external seat is worth its cost, and its findings are still findings to be verified rather than obeyed.
Code-plane external gate (Codex) dispatched. DEVIATION from the skill's default scope, stated: /codex-review-fast baselines on `git diff HEAD`, which here is only the last uncommitted wave — three of the four commits are already in, so the default would have reviewed a fraction and called the change reviewed. Dispatched directly over `6c4605e..HEAD` plus the working tree instead, with a code-review lens (determinism, short-circuiting, boundary conditions, preserved behaviour, interface agreement in both directions, self-application) rather than the doc lens batch 1 already applied. Signal: three commits sit between the skill's baseline and the change's real start.
[REVIEWER_FALLBACK] plane=code_review from=codex to=contract-neutral-reviewer reason=quota | 2026-09-08T04:50:00Z
Codex quota exhausted mid-dispatch on the code plane; it resets at 17:27 Asia/Taipei, roughly 4.5 hours out. The doc plane DID get Codex proper (both batches, batch 0 through three rounds) — only the code plane is affected. Sticky per change: re-reviews do not re-probe.
Per the global CLAUDE.md degradation clause this is conditional, not a free pass: the conclusion is discounted and stated as such, and a high-risk item must not be closed on a fallback verdict alone — it is owed a Codex re-review when quota returns, and that debt goes in the handoff. Whether to ship on the fallback or hold for Codex is the user's call, not mine; the fallback review runs either way so the work is done when they decide.
Code gate (fallback) round 1: ⛔ Blocked. 0 Critical, 3 Important, 4 Minor — and every one of the three Important is the same family the change exists to close, found by reading the checks as an algorithm rather than as prose. I1: a record with two `subject:` lines was genuinely undecided (same input, two verdicts). I2: the `plan` instruction's SELF-REVIEW item still asked only for set equality — an eighth unaligned surface. I3: the spec forbids task-state markers in plan.md and check 7 leans on that, but no surface told a plan author.
Code gate round 1 fix: all seven closed. The fixer put FIELD CARDINALITY in check 9 as sole owner with abstention carve-outs in 10 and 11, and script-checked all thirteen fixtures for regressions.
Code gate (fallback) round 2 re-review: I1/I2/I3/M1-M4 all ADDRESSED, composition of 9/10/11 walked and sound, I3's boundary intact (no check added), regression re-derived by the reviewer's OWN implementation, self-application passes. Universal cardinality ruled RIGHT rather than overreach — but conditionally, on the author-facing surfaces saying so.
Code gate round 2 finding N1: the instruction's amended `invocation:` sentence was not mirrored into templates/tasks.md. That is the THIRD time in this change that an instruction moved and its template did not (3.1's plan.md, I2, now N1) — and it lands exactly on the condition the cardinality ruling rested on. Fix round 2 therefore requires a two-surface sweep of every record rule, not a one-line repair: fixing only the named instance is what let the pattern recur.
PATTERN FOR THE BACKLOG (surface at close-out): "an instruction changed, its coupled template did not" has now recurred three times inside one change, each time caught by a different reviewer and never by the implementer who made the change. The repair that keeps working is a two-surface sweep; the repair that keeps failing is fixing the named instance.
Code gate (fallback) round 4: ✅ Ready. G1 and G2 ADDRESSED; five matrix rows audited cell by cell and the deferral criterion holds for all but D2; row 15's n/a-by-design confirmed correct; regression re-derived by the reviewer's own implementation (13/13 fixture verdicts unchanged); self-application NO BLOCK on this change AND on the archived loosen-plan control; dogfood diff empty; schema validate green.
Fifth recurrence: FOUND but Minor and non-blocking — G1's qualifier went onto the instruction and not onto templates/tasks.md:49-50, which still says a wrapped line 「也不算錯」 unqualified. It does not fail the deferral criterion because the template's own field-cardinality paragraph already covers the only case check 9 blocks on. Two other one-surface rules were checked and cleared.
Deferred observation worth carrying: `###` in a plan, whitespace-after-`]`, and repeated-field-key have NO FIXTURE — the reviewer verified them by reading only. Three rules this change now blocks on with no mutation fixture behind them.
CODE GATE CLOSED on a FALLBACK verdict, not Codex. Two decisions now go to the user rather than being taken here: whether a fallback verdict may close this gate, and whether D2 (check 8 blocks on a rule no author surface states) is pulled in or left as a follow-up.

## USER RULING 2026-09-08 — A' + D2 not pulled in

Direction A' (user's amendment to the controller's proposed A): do 5.3, verify and retrospective NOW while the context is hot, then STOP BEFORE ARCHIVE. Codex re-reviews the code plane when quota returns; findings are fixed inside the still-active change; only after no blocking finding does archive -> push -> PR -> CI proceed.
The user's reason, sharper than the controller's proposal: `archive` is not folder tidying — it asserts "this change is complete and its delta may merge into the canonical spec". That assertion must not rest on a fallback verdict. verify and retrospective are by contract pre-archive artifacts and are best written hot, so they are unaffected. A' takes A's hot-context benefit and C's assurance discipline without either one's cost.
The fallback verdict stands as PROVISIONAL ASSURANCE — sufficient to keep working, not sufficient to declare completion.
D2 NOT pulled into this change (user agreed with the controller's recommendation). It is a real defect — check 8 blocks on more than one `- TDD:` line while neither author surface states it — but it is not caused by the five P1s and not a precondition of fixing them. The 25x3 matrix already found 9 gaps of the same family, so it is a topic, not two sentences: "do all author-facing surfaces completely reflect the actual gate rules". Pulling D2 in invites D3 and D4 next round.
Standing boundary for the rest of this change: no further scope expansion unless a D2-class gap is shown to affect the CORRECTNESS of the five P1 fixes themselves.
Task 5.3: DONE. D5's premise holds on both halves, and the agent distinguished the two questions rather than conflating them: `git tag -l` empty locally AND `git ls-remote --tags origin` empty on the LIVE remote; `origin/main` tracking ref = 5aa19bf, confirmed current against `git ls-remote origin main` rather than assumed; 24 commits ahead including the v2-introducing commit, so v2 has not landed on origin/main. No fetch was run (it would have mutated the ref being measured).
ALL 15 TASKS COMPLETE. Verify artifact dispatched — the first execution of the corrected checker on a real change, and on the change that corrected it.
Verify artifact: ⚠️ PASS WITH WARNINGS. Checks 1-4 and 6-12 pass; checks 8-12 and R1-R4 return no blocking finding; the change does NOT block itself, derived independently. Check 3 (delta specs need sync) is the normal pre-archive state. Check 5 was unsatisfied only because the last wave was uncommitted — now closed by e38e817. Check 6's WARNING is legitimate pre-install residue in docs/superpowers/specs/, not a routing leak.
Three latent ambiguities the verify agent recorded rather than silently resolving — the honest outcome, and the reason this artifact is worth more than a rubber stamp:
  1. Check 3 lists three outcomes but never says whether its ✗ blocks. Ruled non-blocking.
  2. Check 5 says "confirm all code changes are committed" but never says whether uncommitted files are FAIL or warning. Ruled warning.
  3. Checks 8-12 define TASK LINE and `#` heading by first-non-space characters with NO exclusion for HTML comments or fenced code blocks. This change's own tasks.md opens with a 40-line HTML comment; the agent confirmed line by line that it contains no `- [` or `#`-leading line, so it did not fire. A tasks.md with `- [ ] 1.1 ...` inside a comment or a code fence WOULD be miscounted. Same family as D2 — goes to the follow-up.
Record drift found by verify and fixed: task 5.2's control said "22 added" for the errata append; a later correction to the same file made it 33. Rewritten to state the INVARIANT (removals must stay 0) plus the command and base commit to re-derive it, rather than a fixed count that the next append invalidates again.
Final wave committed e38e817 (10 files, +520/-31, verify.md included; verify-last exit 0).
Follow-up registered per the user's ruling: work-map record `task-20260908-author-surface-gate-alignment`, parent task-20260826-superpowers-bridge-next-gen, status TODO. It carries D2, the 25x3 matrix and its 9 gaps (with the file paths that hold the cell-by-cell evidence), the three latent ambiguities verify.md recorded, and the three rules that now block with no fixture behind them. Registered in the WORKTREE's work-map, not the main directory's — main is still at 5aa19bf and its copy predates this change; the branch's copy is the one that flows with the work.
Retrospective landed: artifacts 8/8. Committed b07d571 with the work-map follow-up registration (verify-last exit 0).
STOPPED HERE, per the user ruling. Archive NOT run, nothing pushed. The change remains active and its delta specs remain unmerged, which is the correct state while the code-plane external review is outstanding.
