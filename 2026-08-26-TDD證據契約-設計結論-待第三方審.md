# TDD 證據契約 — 設計結論（待第三方審查）

> 日期：2026-08-26
> 專案：`openspec-schemas`（fork；remote 為 azuma520/openspec-schemas）
> 對象：`superpowers-bridge` schema v1 → v2
> 產出方式：Claude 與使用者對談（含 grilling 拷問），所有事實宣稱皆經工具查證
> **審查請求：請盡量推翻。我們要的是缺陷，不是確認。**

---

## 0. 給不熟悉背景的審查者

`superpowers-bridge` 是一個 **OpenSpec 自訂 schema**。OpenSpec 是規格驅動開發 CLI，讓你定義一個 change 要依序產出哪些文件（artifacts）以及彼此的相依關係。這個 bridge 把 OpenSpec 的「文件治理」和 obra/superpowers 的「執行技能」接起來，整合**完全發生在 prompt 層**（`schema.yaml` 的 `instruction:` 欄位），不改 OpenSpec CLI，也不改 Superpowers 原始碼。

目前的 artifact 流程：

```
brainstorm → proposal → design ┐
                               ├→ tasks → plan → verify → retrospective
                    specs ─────┘
                    apply（在 plan 之後執行）
```

`apply` 階段呼叫 `superpowers:subagent-driven-development`（以下簡稱 SDD），由它派發子代理逐一實作 `plan.md` 的任務。

---

## 1. 觸發這次設計的事實發現

### 1.1 上游漂移（已查證）

bridge 的相容性基準釘在 Superpowers `v5.1.0`，實際安裝為 `v6.3.0`，跨一個 major。

| 查了什麼 | 結果 |
|---|---|
| schema 點名的 8 個 skill 是否還在 | ✅ 全在，無改名 |
| 設計觸點 #4 宣稱 `executing-plans` 不提 TDD 也不提 code-review | ✅ 仍成立（其 SKILL.md 命中數 0）|
| apply 宣稱 SDD 會 transitive 強制 `requesting-code-review` | ✅ 仍成立（每任務一 reviewer + 最終 reviewer，寫在 SKILL.md 結構裡）|
| apply 宣稱 SDD 會 transitive 強制 `test-driven-development` | ❌ **已不成立** |

### 1.2 核心問題：TDD 是懸空的

**SDD 的 `SKILL.md`（v6.3.0，32KB）完全沒有強制 TDD 的條文。** 逐行檢查所有含 "test" 的行，沒有一條要求 RED-GREEN-REFACTOR，也沒有把 `test-driven-development` 列為必用子 skill。

所有 TDD 字樣都在 `implementer-prompt.md`，而且**三處全是條件句**：

- `Write tests (following TDD if task says to)`
- `Did I follow TDD if required?`
- `TDD Evidence (if TDD was required for this task)`

**TDD 之所以還會到達實作者，純粹是因為 `writing-plans` 把微步驟寫進每張任務：**

```
Step 1: Write the failing test
Step 2: Run test to verify it fails
Step 3: Write minimal implementation
Step 4: Run test to verify it passes
Step 5: Commit
```

### 1.3 這造成一個危險的耦合

使用者原本的訴求是「前沿模型不需要被寫成 2-5 分鐘一步的施工腳本，那樣反而綁住它」，想放寬 `plan.md`。

但 plan 的微步驟同時扛著兩件事：

- **多餘的**：替模型決定實作路徑（開哪個檔、用什麼指令）
- **必要的**：要求 TDD

**放寬 plan 而沒有替代管道，會靜默地把 TDD 一起拿掉。**

### 1.4 現行 schema.yaml 有一句有害的指令

第 507-513 行：

> **IMPORTANT — transitive skill activation:** subagent-driven-development internally enforces the following skills, **so you do NOT need to invoke them manually:**
> - **superpowers:test-driven-development** — every task follows RED-GREEN-REFACTOR. Implementation code written before a failing test is deleted.

關鍵在 `so you do NOT need to invoke them manually`。這不只是描述錯誤——**它是一句主動叫 agent 放手的指令，而放手之後沒有人接住。**

---

## 2. 設計原則

### 2.1 規定證據，不規定步驟

> 模型擁有路徑，harness 擁有證據和不可逾越的邊界。

篩選判準（用來檢查 plan / tasks 裡的每一句）：

> **如果兩個優秀的 agent 可以用不同做法、最後都符合規格，那計畫就不該提前替它們選一個。**

- 留：`必須 MIT 授權`、`schema 必須 validate 通過`、`不得修改 OpenSpec CLI`、`必須做 TDD 並附 RED/GREEN 輸出`
- 刪：`用 Write 工具建檔`、`跑這串 /tmp 指令驗`、`分成 9 個 commit`

### 2.2 解耦分層

| 層 | 擁有什麼 | **不該知道什麼** |
|---|---|---|
| 規格層（Requirement + Scenario）| 要什麼、怎樣算對 | 誰去做、改哪個檔、跑哪個測試 |
| 任務層（一張 task = 一次測試循環）| 執行證據：RED/GREEN、指令、exit code、commit SHA | 整個 change 完成了沒 |
| 變更層（verify）| 完整性：答應的事有沒有全做到 | 怎麼做到的 |

**推論：Evidence Matrix 不是一份要人填的表，是一次「對照」。** 它不生產新證據，只把規格層與任務層接起來，看有沒有落單的。

---

## 3. 設計決策

### D1. 驗收的真相來源 = spec 的 Scenario

沿用 OpenSpec 原生機制，不另外發明。原生規則：每個 Requirement 至少一個 Scenario，且官方明言「each scenario is a potential test case」。

**實測支撐**：使用者既有 1,020 個 scenario 中，83% 含 MUST/SHALL 規範動詞，77% 點名具體檔案/指令/marker，**65% 兩者兼具、可直接寫成斷言**。前期成本接近零。

### D2. 鑰匙放在 `tasks.md`，單向往上指

每張任務標明它滿足哪些 Requirement。**規格層不反向指任務**（否則規格會知道誰在做它，違反分層）。

**現況**：OpenSpec 原生 `tasks.md` 是純 checkbox，無任何欄位；bridge 的模板同樣提到測試/驗收/證據 0 次。原生 verify skill 補這個洞的方式是「用關鍵字去程式碼裡猜」（`search codebase for keywords... assess if implementation likely exists`），只開 WARNING 不擋。

### D3. 鑰匙形狀 = 原樣照抄 Requirement 標題

**CLI 幫不上忙（已查證）**：`openspec show <spec> --json` 的 requirement 物件只有 `text`（內文）與 `scenarios`，**標題完全未被暴露**；CLI 自己用 `--requirement <id>`（1-based 序號）指涉需求，而序號會因插入或 archive 合併而位移，不能當鑰匙。

因此只能在 prompt 層做。**verify 做完全比對，不做模糊比對**——抄錯就報「對不上」。

理由：完全比對**失敗會大聲**（表上出現缺口）；模糊比對**失敗會安靜**（看起來對上了但對錯）。後者正是使用者記錄過三次的「假綠」事故形狀。

使用者既有 306 個 Requirement 中僅 1 個帶編號（0%），全部以整句話為名。名稱偏長是已知代價，使用者判斷「這是給 agent 用的，冗長不構成負擔」。

### D4. 證據存放 = `openspec/changes/<name>/evidence.md`

單一檔案，每張任務追加 4-5 行：

```markdown
## Task 3 — 實作 backlog 完成標記

**Requirement**: `Completed items MUST use [done: YYYY-MM-DD] marker with cap-10 FIFO`
**RED**  `pytest tests/test_backlog.py::test_done_marker_fifo` → FAILED: AttributeError: no attribute apply_done_marker
**GREEN** 同指令 → PASSED (14/14, output pristine)
**Commit** `a3f2c19`
```

**存濃縮事實，不存原始逐字稿。** 理由：原始輸出的價值在實作當下與 verify 時很高，三個月後幾近於零；要查假的會去**重跑指令**，不會讀當初的 stack trace。

**必須解決的上游衝突（已查證）**：SDD 的工作區是 git-ignored，且流程圖上有一個正式節點叫 `Final review clean: delete this plan's workspace`，SKILL.md 明寫「刪掉工作區——記錄已經在 git 裡了」。該假設對程式碼成立、對證據不成立（commit 裡沒有「實作前看到紅燈」那份輸出）。

**應對方式：不搶救工作區**，改為由 controller 在每張任務 review 通過後、順手把 4-5 行抄進 `evidence.md`。理由：controller 當下手上就有那份報告，成本最低；且與工作區的生死解耦，上游改機制不影響我們。

**規模實測**：使用者既有 change 的 scenario 數中位數 18（最多 143）。若逐 scenario 要求證據會過重並誘發造假；按 Requirement 記約 5-6 行/change，按 Task 記 RED/GREEN 則與 `writing-plans` 對 task 的定義（「能擁有自己一次測試循環的最小單位」）天然對齊。

### D5. Scenario 不獨立成行（結構性理由）

要做 scenario 層對照，就得讓測試宣告它涵蓋哪個 scenario（例如測試名稱對應 scenario 名稱），等於把測試與規格措辭綁死——**規格改個字，測試就得跟著改名，否則對照就假**。

這正好是使用者全域規範在防的東西：「先讀『實際驗到什麼』再讀『名字說驗什麼』……兩者對不上就是一個 finding」。用結構逼所有測試掛上 scenario 名字 = **大規模製造**那種對不上。

### D6. schema graph 升 v2

`evidence` 列為正式 artifact，`verify.requires` 由 `[plan]` 改為 `[evidence]`。

**問題**：引擎只檢查前置檔案存不存在。`plan.md` 一寫完，`openspec status` 就把 `verify` 標成 `ready`，而 `/opsx:continue` 的行為是「挑第一個 ready 的把它生出來」——**這時一行程式都還沒寫**。目前僅靠 verify instruction 開頭的 PRECHECK（跑 `git log` 數 commit、數 tasks 打勾）攔截。

**每條邊的可辯護性**：

| 邊 | 成立嗎 |
|---|---|
| `verify requires plan` | ❌ verify 要的是實作，不是計畫書 |
| `evidence requires plan` | ✅ 沒有計畫就沒東西可執行 |
| `verify requires evidence` | ✅ verify 就是在查證據齊不齊 |

`evidence` 是唯一真正從 apply 長出來的東西，適合當橋樑。**謊言沒有完全消失（仍可產空檔案），但變小了，且整條鏈每段都講得通。PRECHECK 仍保留。**

**已查證的引擎限制**：OpenSpec 1.3.1 的 schema 格式（zod 定義）合法欄位僅 `{name, version, description?, artifacts[], apply?}`，artifact 僅 `{id, generates, description, template, instruction?, requires[]}`。**沒有 `post_apply`，沒有任何 phase 概念。**「等上游出 post_apply」在可預見時間內不是選項。

**代價**：schema major 1 → 2、bundle 由 `2.0.0` 起算、需 migration note。使用者以「這是我個人的 fork」為由接受。

### D7. verify 對不上時是 blocking（FAIL），非 warning

沿用使用者既有裁定：三軸（規格/實作/測試）全部強制、**不允許 N/A**、任一軸缺就退回補 spec、不放行。允許「同軸內降級寫法」（純重構類的測試軸可寫「跑既有 test suite 全綠 + 真站 smoke 對比 baseline 無退化」，算回歸測試，非 N/A）。

---

## 4. Roadmap

| 順序 | Change | A → B |
|---|---|---|
| 1 | 修正 TDD 保證的錯誤宣稱 | schema 主動叫 agent 別管 TDD → 據實說明 TDD 是條件性的 |
| 2 | TDD 證據契約（升 v2）| 沒有任何地方要求或保存證據 → 任務指向需求、留下證據、verify 確定性對照 |
| 3 | plan 放寬 | 規定步驟 → 規定契約 |
| 4 | brainstorming 三路徑漂移 | schema 描述與上游行為不符 → 相符 |

之後才是階段二（Orca：`execution-readiness` / Coordinator / 派工判準），本文不涵蓋。

**Change 3 排在 Change 2 之後的理由**：plan 放寬的風險完全依賴證據契約先成立。先放寬等於先拆保險絲。

**另一個已知漂移（Change 4 的內容）**：`brainstorming` v6.x 開頭會把請求分類成 spike / bounded / architectural 三條路，**只有 architectural 那條會執行 schema 所描述的五個步驟**；另兩條只在對話中給簡短結論就停住，導致下游 `design.md` 要重組出 Context/Goals/Decisions/Risks/Migration 時沒有素材。此外 v6.x 明載 architectural 之後唯一該調用的是 `writing-plans`，而本 schema 在中間插入 proposal → design → specs → tasks。

---

## 5. change 邊界判準（本次一併檢討）

使用者既有判準：

> Issue = 我們需要解決的問題；Change = 我們要讓系統有怎樣的變化；Task = 為了變成那樣，我們實際要做什麼
> 一個好的 Change，應該小到可以被獨立理解、實作、驗證、接受或拒絕。
> Change 完成 = 期望的系統狀態已經成立，而且驗證通過。

**壓力測試結果**：

- 擋得住太大的（「改善訂購系統」答不出「做完了嗎」）✅
- **擋不住太小的**（「把 LICENSE 年份改成 2026」四個條件全過）❌
- **無法在兩個都合法的切法之間選擇**（「1+2 分開」與「1+2 合併」全部通過）❌

**補上缺口的機制（皆為既有，散在別處）**：

| 面向 | 來源 | 可機械化 |
|---|---|---|
| 該不該走 opsx | 使用者三軌制 | 半判斷 |
| 下界：至少改動一條需求 | OpenSpec `CHANGE_NO_DELTAS` | ✅ CLI 驗 |
| 上界：≤10 delta、Why ≤1000 字元 | OpenSpec `MAX_DELTAS_PER_CHANGE` 等常數 | ✅ CLI 驗 |
| 怎麼切 | 判準在此沉默 | ❌ |

**提議的修正版**（使判準能做選擇）：

> 一個好的 Change，是**在所有「每塊都能獨立驗證」的切法裡，最細的那一種**——除非分開的價值抵不過多跑一次完整流程（8 個 artifact）的成本。

---

## 6. 我們希望被攻擊的地方

請優先針對以下各點嘗試推翻。**若同意，也請說明在什麼條件下會失效。**

1. **D4「存濃縮事實不存逐字稿」是否弱化防偽能力？** 我們的論證是「逐字稿也擋不住說謊，只是讓說謊變麻煩；真正擋假綠的是獨立 reviewer 看 diff 與 verify 抽樣重跑」。這個論證站得住嗎？
2. **D4 要求 controller 在每張任務後抄寫證據——是否在跟上游設計對抗？** 上游明確要刪工作區。我們的做法是否會在 SDD 未來改版時斷裂？
3. **D3「原樣照抄長標題」在實務上會不會退化成改寫？** 若 agent 改寫措辭，完全比對會報大量假缺口，可能反而誘使人放寬成模糊比對。這個滑坡風險多大？
4. **D6 升 v2 是否划算？** 我們承認「謊言只是變小沒有消失」。為了一條可辯護的邊，付出 major 版本 + migration note，值得嗎？是否該等階段二（Orca）一起做？
5. **D1 把驗收綁在 Scenario 上，是否會讓 spec 階段成為瓶頸？** 實測顯示該使用者既有 scenario 已 65% 可直接斷言，但這個結論可否推廣到其他採用者？
6. **整體是否過度工程？** 核心問題是「TDD 會被靜默拿掉」。是否存在**明顯更便宜**的解法（例如只在 apply instruction 加一句「所有任務一律要求 TDD 並附 RED/GREEN 證據」）能達成同等效果？如果有，我們為什麼看不見它？
7. **第 5 節的修正版判準（最細切法 + 儀式成本制動）是否可操作？** 「儀式成本」如何量化？會不會只是把判斷推到另一個模糊詞上？

---

## 7. 附：已查證事實清單（供審查者交叉驗證）

| 宣稱 | 查證方式 |
|---|---|
| SDD SKILL.md 無 TDD 強制條文 | 逐行列出 32KB 全檔所有含 "test" 的行 |
| implementer-prompt.md 三處 TDD 皆條件句 | 全文讀取 |
| SDD 會刪除 git-ignored 工作區 | SKILL.md 第 90/119/120/137/153/483/565 行 |
| OpenSpec 無 post_apply | 讀 `dist/core/artifact-graph/types.js` zod 定義 |
| CLI 不暴露 requirement 標題 | 實跑 `openspec show --json` 並與 spec.md 原文對照 |
| `MAX_DELTAS_PER_CHANGE = 10` 等常數 | 讀 `dist/core/validation/constants.js` |
| `/opsx:continue` 挑第一個 ready 的 artifact | 讀 `.claude/commands/opsx/continue.md` |
| 1,020 scenario 中 65% 可直接斷言 | 對 36 個 spec 檔正則統計 |
| 306 requirement 中 0% 帶編號 | 同上 |
| change 的 scenario 數中位數 18 | 對 57 個歸檔 change 統計 |
