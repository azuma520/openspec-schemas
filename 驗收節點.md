<!--
workflow-harness — 驗收節點.md template
對應 capability：observation-checkpoint
位置：使用者專案根 驗收節點.md（檔名先前為 observations.md、由 refactor-observation-system change 改名）

規矩：
- sentinel 注解格式不可改：HTML 注解、開始標記 pending-verify:start、結束標記 pending-verify:end
  hook 寫死讀英文 sentinel、用 grep 抓「到日驗收條目」（跨平台無 unicode 坑）。
- legacy 舊版 sentinel observation:start / observation:end 由 parser 接受 read-only、一個 major version 過渡期；`/init-harness --force-replace` 會自動改寫成新版。

（此 doc comment 內**不**字面寫 HTML sentinel 標記、避免 outer comment 因 inner 結束符提前關閉、以及 grep parser 誤匹配。實際 sentinel 標記在本檔下方真實 sentinel 區段、見 ### 近期 / ### 遠期 兩 section 之外的開始 / 結束標記行。）

Inline schema（每條一行、4 欄、` | ` 分隔）：

  - [ ] YYYY-MM-DD {一句話描述} | acceptance: {可量化條件} | method: {驗收手段} | result: —

4 欄對應 SMART 5 元素：
- YYYY-MM-DD = Time-bound（T）— 驗收 due date（user-decided、不是建立日期）
- {一句話描述} = Relevant（R）— 這次驗收什麼工作
- acceptance = Specific + Measurable（S + M）— 必含具體數字 / 條件 / 閾值；
  例：「曝光升 70%」/「pos 進前 7」/「早起天數達 21/30」；
  避免「有起色」「順一點」「變好」這類無數字、無條件的措辭（agent 會 ⚠️ Warn、不 block）。
- method = （沒對應 SMART 字母、但補一條：怎麼看才看得到）
  用詞通用、跨自動化光譜：自動腳本 / 半自動 / 純手動 / 訪談 / 量測 / 感官 / 純觀察均可。
  不寫死「腳本」一詞。
- Achievable（A）= acceptance 跟 deadline 匹配性、agent 在 plan-stage 提示（acceptance 70% 但 deadline 3 天 → ⚠️）、不強制 spec block。

result 欄：建立時填 `—`（em dash U+2014、不是 hyphen `-`、不是空字串、不是 "TBD"）；
驗收日 user 填具體結果（例：`✅ 達標 (3 次/30 天)、handoff 0625 Session 2`）。

驗收節點 ≠ 死檔：本檔的驗收條目 + 結果寫回、跟 handoff【當日洞見】共同構成議題 15 週報的 primary qualitative source。
驗收節點 = 通用工作驗收容器：不只服務 plugin 升級 close loop、也接「自己改 SEO 想驗效果」「想看新習慣有沒有撐住」這類一般工作。
-->

# 驗收節點

> 成效驗收容器（PDCA 的 C 階段）。由 `/pending-verify`（別名 `/observe`）寫入、SessionStart hook 到日 surface、`/end-session` 收尾打勾。

> Inline schema：`- [ ] YYYY-MM-DD {描述} | acceptance: ... | method: ... | result: —`

> 結果寫回時填 `✅ 達標` / `❌ 未達` / `🟡 部分 / 延期` + brief 結果字串 + handoff back-link。

---

<!-- pending-verify:start -->
## 驗收節點（成效驗收）

> 此 section 是 sentinel 區段；包夾本 section 的 HTML 注解標記（pending-verify:start / pending-verify:end）**不可改**。

### 近期（近期天數可在 `config/defaults.yaml` `backlog.near_term_days` 配置、預設 7 天）

- [ ] YYYY-MM-DD （內容描述）| acceptance: （可量化條件）| method: （驗收手段）| result: —

### 遠期

- [ ] YYYY-MM-DD （內容描述）| acceptance: （可量化條件）| method: （驗收手段）| result: —
<!-- pending-verify:end -->
