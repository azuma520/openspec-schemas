# TDD 假保證 — 第三方審查結果與改寫建議

> 2026-08-27。審查對象：`openspec/changes/fix-tdd-transitive-claim/brainstorm.md`（commit `f8a1455`）
> 審查者：Codex `codex-cli 0.149.1`
> 結論：**不通過**。2 個 P0、7 個 P1、2 個 P2
>
> **本檔是討論素材，不是決議。** 建議欄位皆為草案，待下個 session 白話討論後才定案。

---

## 〇、怎麼跑的（可重現）

`sd0x:doc-review` 當時跑不動——它第 2、3 步要 `node scripts/check-doc-links.js` 與
`resolve-review-profile.js`，而本 repo 尚未安裝 sd0x 腳本。故直接呼叫 Codex：

```bash
codex exec --skip-git-repo-check "<審查題目>"
```

審查題目明確要求五件事：①宣稱 vs 出處逐條查證 ②有沒有製造新的假保證
③範圍宣稱是否成立 ④設計本身的漏洞 ⑤該做沒做的。並要求分級、附行號、
標明「讀出來的」還是「實際跑/查出來的」。

> 事後補記：sd0x-dev-flow 4.3.1 的規則與腳本已於 commit `131efe9` / `3b48843` 裝入本 repo，
> 下次可改走 `doc-review` 的多檔批次流程。

---

## 一、抽驗：我自己查了哪幾條

收到審查意見不盲目照做。抽驗四條最關鍵的，**四條全部成立**：

| 抽驗項 | 驗證方式 | 結果 |
|---|---|---|
| 上游是否真的禁止用 grep 當測試 | 讀 `writing-good-tests.md` | ✅ 成立，原文見 §二 P0-1 |
| 上游是否把 configuration files 列為 TDD 例外 | 讀 `test-driven-development/SKILL.md:16-28` | ✅ 成立 |
| TDD false row 是不是 `c134f1c` 加的 | `git log -S "False in v6.3.0"` | ✅ 審查者對，實為 `3156806` |
| `agy` 版本是 1.1.16 還是 1.1.21 | 重跑 `agy --version` | ✅ 審查者對，現為 `1.1.21` |
| README 是否已釘 `v5.1.0` | 讀 `README.md:485-487` | ✅ 表格列 `\| v1 \| 1.3.1 \| v5.1.0 \| 2026-05-11 \|` |
| 8/26 審查是否已警告「禁 N/A 誘發假證據」 | 讀該檔 `:174` | ✅ 逐字吻合 |
| checklist 是否說 7/8/9 非 grep 型 | 讀 `propagation-checklist.md:56,58` | ✅ 逐字吻合 |
| `evals.json` 的覆蓋範圍 | 讀全檔 | ⚠️ 大致成立，但需修正措辭——見 P1-8 |

**八條抽驗，七條逐字成立、一條需修正措辭。** 對其餘未抽驗項的信心因此提高，
但仍屬「讀出來的」，非逐條實證。

---

## 二、Findings 全文與建議

### P0-1　我們的 TDD 定義與上游的 TDD skill 正面衝突

**審查發現**

`brainstorm.md` Q2 決定：「凡可判定的檢查都算」——單元測試、`schema validate`、
grep 斷言、`diff -r`，一律要求 RED→GREEN。

但上游 `superpowers:test-driven-development` 明文：

```
| Test a script or document | Run it / pressure-test its consumer; never grep its text |
```
（`writing-good-tests.md` 表格列，同段另有 `trivial code and human prose earn none`）

且 `test-driven-development/SKILL.md` 的 When to Use 段：

```
**Exceptions (ask your human partner):**
- Throwaway prototypes
- Generated code
- Configuration files
```

**衝突點**：設計同時要求執行者「invoke 上游 TDD」與「做上游明令不算 TDD 的檢查」，
兩者無法同時遵守。另 `diff -r` 只是變更偵測器，沒有驗收 oracle。

**⚠️ 延伸發現（審查未點名，但由其引文直接推出）**

`schema.yaml` 就是 configuration file。依上游規則，改它的正解是
**「問人類」**，不是硬套 RED/GREEN。也就是說——**本 change 自己就落在上游的例外條款裡**。

**建議（草案）**

把「證據」拆成兩類，各有各的名字、判準與適用範圍，**不共用 TDD 這個詞**：

| 類別 | 名稱草案 | 怎麼做 | 適用 |
|---|---|---|---|
| A | **行為證據** | 跑 consumer / pressure-test，RED→GREEN 成立 | 有可執行行為的改動 |
| B | **殘留證據** | 全域搜尋比對、範圍凍結、雙向核對 | 「同一句話散落多處」的文字一致性改動 |

並明確寫：**B 不是測試，不得宣稱它是**。

**為什麼**

1. 混用會讓執行者收到互相矛盾的指令——這比沒有指令更糟，因為它會逼人二選一而不留痕跡
2. 把 grep 叫成 TDD 會製造**假的 RED**：文字改完前搜得到、改完後搜不到，看起來像紅轉綠，
   但它從頭到尾沒有「這樣做對不對」的判準
3. 保留 TDD 這個詞給真正的行為測試，才不會稀釋它——而 TDD 是使用者用這整套的理由

**還沒解決的**：configuration file 例外要怎麼處理？照上游是「問人類」，
但本 repo 的產物幾乎全是 configuration 與 prompt。**這題留給下次討論。**

---

### P0-2　我們自己造了一個新的假保證

**審查發現**

Q3 的分層表把「CI 自動檢查」與「沒有 Propagation Check Report 就不算修完」
兩列都標為 **✅ 擋得住**。但：

- §4.2 未指定任何 CI job、wrapper、檢查程式、失敗條件或不可偽造的證據
- §5 明確寫「不做自動殘留檢查腳本」
- 現行 CI（`validate-schemas.yml`）只跑 `openspec schema validate` 與 `openspec schemas`
- OpenSpec 1.3.1 `archive.js:162-180`：`--yes` 遇未完成任務仍直接繼續

**報告字串只是 prompt 政策，不是閘門；而且「有報告」不等於「真的跑過」。**

**這是同一種病在同一份文件裡當場復發**——在修「把建議寫成保證」的文件裡寫了一個保證。

**建議（草案）**

分兩步，第一步本 change 做，第二步另立：

1. **誠實降級**：那兩個 ✅ 改成「⚠️ 政策層，擋不住」。同時把「唯一真的擋得住的是
   會失敗的自動關卡」這句話寫進文件，讓下一個讀的人不會再誤會
2. **真閘門另立一件工作**：做一個會 fail 的 CI check

**為什麼**

先做 1 而不是直接做 2，是因為**做閘門必須先定義「證據長什麼樣」**——而證據的形狀正是
P0-1 還沒解決的題目。順序反了會做出一個擋錯東西的關卡，比沒有更糟。

---

### P1-1　「10 處」算錯，實際至少 28 段

**審查發現**

依 brainstorm.md §2.2 自己列的粒度重算：

| 檔案 | 組數 |
|---|---|
| `schema.yaml` | 4 |
| `README.md`（英） | 7 |
| `README.zh-TW.md`（繁） | 7 |
| `templates/retrospective.md` | 1 |
| **小計** | **19 組** |

即使把中英 README 視為同一邏輯位置，也是 **12 組**。**沒有任何算法得到 10。**

實掃另找到表中漏列、依賴相同錯誤前提的 **7 段 fallback 理由**：
`schema.yaml:520-523`、兩份 README 各 `310` / `449` / `553`；
另兩份 README `:506` 的「TDD 已不再無條件」也要修。

**受影響段落至少 28 組。**

**建議（草案）**

用剛裝入的 `.claude/rules/scope-discipline.md` 三個機械條件重算範圍，並產出
**變更面清單（Affected Surface）**，**算一次就凍結**，後續任何路徑不得重算。

**為什麼**

那份規則已經把「範圍」機械化了，而且它的紀律與使用者既有的判準同源：
**不確定 ⇒ fail-closed 當作在範圍內；要宣告不在範圍內，必須提出三個條件各自為假的完整反面證據。**
我們手上一直有這個工具，只是今天才裝進來。

---

### P1-2　`retrospective.md` 不是「只是講錯」

**審查發現**

`templates/retrospective.md:59-64` 會要求**預設全部打 ✓、打 ✗ 必須解釋**。
它不只是描述錯誤，**它會誘導產生假的合規紀錄**。

**建議（草案）**

把它從「描述型」重新歸類到「會被照著做的」那一類，比照解除型處理：
不能只把文字改對，要連「它會讓人做出什麼動作」一起改。

**為什麼**

原本的兩類切分（解除型／描述型）判準是「這句話會不會叫人停手」。
`retrospective.md` 不叫人停手，但**它叫人打勾**——同樣是驅動行為的句子。
所以是判準本身漏了一種情形，不是分類分錯。

---

### P1-3　「文字可窮舉 / 行為不可窮舉」的切分錯誤

**審查發現**

Q4 把**載體**當成可機械化的判準。但：

- 文字的**語意改寫、同義句、跨語言翻譯**不能靠精確搜尋窮舉
- 許多**行為**可以用執行、pressure test、mutation test 機械驗證

更直接的反證：brainstorm.md 自己引用的 `propagation-checklist.md:56-58` 明寫
**維度 7/8/9 不是 grep 型檢查**；但 §Q7 又寫「同類殘留不派人、用搜尋」，
與 §4.2「7/8/9 必須外部審」自相矛盾。

**建議（草案）**

改用 **「判準能不能機械判定」** 切分，不按文字／行為切。

**為什麼**

可判定性取決於**判準的形狀**，不是內容的載體。
「這句話有沒有出現」可判定；「這句話是不是在超額承諾」不可判定——兩者都是文字。
「跑起來會不會壞」可判定；「架構好不好」不可判定——兩者都是行為。

---

### P1-4　「綁出處就能偵測漂移」是另一個假保證

**審查發現**

檔案、行號、日期只提供**可追溯性**；沒有定期重抓、內容雜湊、版本 pin 或 diff job，
**不會自行偵測漂移**。

**建議（草案）**

措辭改成「**使人工重驗可重現**」。

若要真的偵測：現有的 `version-check.yml` 每週會比對上游版本號，
可擴充成比對「我們宣稱的那幾句，出處還在不在、還是那樣寫的嗎」。
**列為候選工作，不在本 change 承諾。**

**為什麼**

這是第三個假保證。三個都出自同一個習慣：**把「有幫助」寫成「能保證」**。
措辭降級的成本是零，而誤導的成本是下一個人不去做真正該做的事。

---

### P1-5　§2 有四處宣稱與來源不符

| # | 原文寫的 | 實際 |
|---|---|---|
| 1 | 「6.3.0 所有 TDD 字樣都在 `implementer-prompt.md`」 | **錯**。`task-reviewer-prompt.md:75` 也有——而且是我自己稍早引用過的那一行 |
| 2 | 「`internally enforces` 在上游**任何一版**都找不到來源」 | 只查了 5.1.0 / 6.2.0 / 6.3.0，最多只能說「**已查的三版**」 |
| 3 | 「Compatibility 表那列由 `c134f1c` 修正」 | **錯**。`git log -S` 證實由 `3156806` 加入；`c134f1c` 修的是下一列的 code-review 誇大 |
| 4 | 「bridge 當初對照哪一版未查」 | `README.md:485-487` **早已明列 `v5.1.0`**，不該留為未查 |

**建議（草案）**

逐條訂正。第 4 條訂正後，§2.1 的結論可以講得更滿：
**基準版即已誤讀**，而非「後來漂移」。

**為什麼**

第 1 條特別值得記：**我在同一次對話裡先引用了那一行當反向證據，
半小時後又寫下一句排除它的絕對句。** 這正是「絕對斷言前先找一個反例」那條規矩要防的事，
而反例當時就在我自己的輸出裡。

---

### P1-6　Q1 與 Q2 互相打架，契約沒有可執行的射程

**審查發現**

- Q1 只要求「**會改變行為的**任務」附 TDD
- Q2 又說「不改變行為」會成為好按的逃生鈕，因此**否決**二分宣告

**那最後由誰、在何時、按什麼例子判斷？** 本 repo 的主要產物正是 YAML、Markdown、prompt，
邊界不是小問題。否則 agent 不是隨意逃逸，就是**被迫造假 RED**。

**建議（草案）——這條需要使用者拍板**

至少要定義：分類表、爭議升級給誰、以及純散文／configuration／可執行 prompt 三者的處置。

兩個方向供討論：

- **甲**：定義一張分類表，逐類指定證據形態（含「不需要證據」也要明列，並說明為什麼不是逃生鈕）
- **乙**：不分類，改成「每張任務都要說出它的驗收判準是什麼」，判準寫不出來就退回

**為什麼**

甲的風險是清單外的東西沒歸屬；乙的風險是「判準」可以寫得很鬆。
但乙有一個結構上的好處：**它不允許「不需要證據」這個答案存在**，
而 Q2 當初否決二分宣告的理由正是那個出口太好按。

---

### P1-7　§4.2 的五條是意向，不是可實作的設計

**審查發現**

五條都沒回答：寫在哪個 artifact、誰讀、缺少時誰阻擋、外部審查者被跳過或不可用時
apply 是停止還是降級。

而且：**「schema 引用某個 skill」不會讓那個 skill 執行**。
`verification-before-completion` 的 frontmatter 只約束「完成宣稱」，
**沒有替每張任務產生 TDD 證據**。

**建議（草案）**

每一條補齊五欄：**目標（寫在哪）／觸發（何時）／輸入／輸出／失敗語意（缺了怎樣）**。

**為什麼**

這一條和 P0-2 是同一個病的兩種長相：**P0-2 是把政策說成閘門，P1-7 是把引用說成執行。**
兩者都預設「寫下來就會發生」。

---

### P1-8　§七的「未讀材料」會直接改變設計，不能留待日後

**審查發現**

| 材料 | 它會改變什麼 |
|---|---|
| `2026-08-26-TDD證據契約-Codex審查與查證.md:170-175` | 已明列「**嚴格禁止 N/A 會誘發假證據**」——**Q2 原樣重犯** |
| `propagation-checklist.md:56-58` | 明確推翻 Q7 的「同類殘留全部用搜尋」 |
| `review-fix-propagation/evals/evals.json` | 只有 **4 個 eval**、`iteration: 1`；本設計最倚重的維度 **7/8/9 沒有 eval、沒有結果** |

⇒ 這些維度**有 dogfood 紀錄，但不能據此稱為「已驗證的成熟閘門」**。

**我方查證的精確度說明**（引用 vs 推論要分開）：

- **引用**：`propagation-checklist.md:56` 標題直接寫「**只涵蓋維度 1-6**」，
  第 58 行明文「維度 7、8、9 **不是 grep 型檢查**……**看完本檔 ≠ 跑完 Step 3**」
- **引用**：`evals.json` 有 4 個 eval，`iteration: 1`
- **推論（非引用）**：四個情境分別是 rename / heading schema / marker tag / command cross-ref，
  **全屬字串傳播家族**。`evals.json` 本身**沒有寫**「涵蓋維度 1-6」這句話——
  「7/8/9 未被 eval 覆蓋」是由情境性質推出的，不是文件自陳

**⚠️ 另一項查證中發現、審查者未提的事**

`evals.json` 的 `design_notes` 自陳：Codex 5/21 指出的 5 個問題中，**第 2 項（fixture）被跳過**，
理由是「**subagents describe their plan instead of actually grepping**」——
因為 plugin repo 沒有虛擬 fixture 檔案。

也就是說：**這 4 個 eval 驗的是「agent 有沒有說出正確的計畫」，不是「agent 有沒有真的去搜」。**
連維度 1-6 的 eval 覆蓋，強度也比表面看起來弱。

這一條對本設計的影響大於審查者原本點出的那條——我們原本想把這份 skill 當支柱。

**建議（草案）**

改寫前先讀完這三份。並把「維度 7/8/9 已驗證」的措辭降級為「有累積案例，未經 eval 驗證」。

**為什麼**

第三條特別重要：**我們把那份 skill 的維度 7/8/9 當成整個設計的支柱，
而那三個維度恰好是它自己沒有 eval 覆蓋的部分。** 這又是「讀了名字沒讀實際驗到什麼」。

---

### P1-9　§4.4 的 fallback 不能放在未決，且不可偷渡成 bundle 需求

**審查發現**

- brainstorm.md 已決定 `Codex → Fable`
- 但全域 `CLAUDE.md` 現行仍是 `Codex → strict-reviewer / 確定性驗證 → 必要時 Fable`
- **本 change 的審查路由現在就會受衝突規則影響**

另有一條更重要的：**必須先決定這是「本機開發的前提條件」還是「bridge 對採用者的契約」。
不可把個人的 Codex / Fable 可用性偷偷變成 bundle requirement。**

**建議（草案）**

1. 明確標記為**本機開發前提**，不進 bridge 的對外契約
2. 同步更新全域 `CLAUDE.md` 該條，或明確標註「本 repo 例外」

**為什麼**

第 2 點是使用者自己的規矩，改了不同步就會出現「規矩與實作不一致」——
而那正是本 change 在修的病的一般化版本。

第 1 點更關鍵：採用者不一定有 Codex 或 Fable。
把「要有 Codex」寫進對外契約，等於**再造一個他們達不到的保證**。

---

### P2-1　「strict-reviewer 結構上抓不到」仍是把機率寫成保證

**審查發現**

它的預設 rubric 確實偏程式碼，但其定義會讀完整的 changed file，
且有 `Read/Grep/Glob/Bash`（`strict-reviewer.md:11-17, 37-42`）。
**題目給對，它不是結構上不可能抓到。**

另「Codex 的程式碼審查流程是同一組尺」**沒有引用**，且 Codex prompt 本來可自訂。

**建議（草案）**

改寫成「**預設題目下高機率漏掉**」，並刪掉沒有出處的那句。

**為什麼**

這是本文件的第四個假保證，只是方向相反——**把「大概不會」寫成「一定不會」**。
過度肯定與過度否定是同一個毛病。

---

### P2-2　工具版本不是穩定的出處

**審查發現**

§2.5 沒附命令輸出或時間。實跑 `codex --version` = `0.149.1`（吻合）；
但 `agy --version` 已是 **`1.1.21`**，不是文中的 `1.1.16`。
且版本命令**不能證明指定的遠端模型真的打通**。

**建議（草案）**

要嘛保存實跑命令、時間與成功判準；要嘛刪掉版本細節，只標成「當次環境觀察」。

**為什麼**

`agy` 在同一個工作階段內從 1.1.16 變成 1.1.21——**它會自己更新**。
把自動更新的東西的版本號寫進設計文件當事實，保存期限以小時計。

---

## 三、改寫方針（草案）

**不打補丁，重寫。** 因為 P0-1 與 P1-3 動到的是**問題的切分方式**，不是細節。

改寫順序建議：

1. 先讀完 §P1-8 那三份材料
2. 重新切分「證據」的兩類（P0-1），並處理 configuration file 例外
3. 依 `scope-discipline.md` 三條件重算範圍、產出凍結的變更面清單（P1-1）
4. 修正兩類缺陷的判準，納入「會叫人打勾」這一種（P1-2）
5. 把可判定性改成判準導向（P1-3）
6. 誠實降級四個假保證（P0-2、P1-4、P2-1，以及 P1-7 的「引用≠執行」）
7. 訂正四處事實錯誤（P1-5）
8. 補齊五條的五欄規格（P1-7）
9. 標記 fallback 為本機前提、不入對外契約（P1-9）

---

## 四、需要使用者拍板的（下次白話討論）

| # | 問題 | 為什麼需要你 |
|---|---|---|
| 1 | **configuration file 例外怎麼辦？** 上游說「問人類」，但本 repo 產物幾乎全是 configuration 與 prompt | 這是「值不值得、邊界在哪」的判斷，不是查得到答案的事 |
| 2 | **P1-6 的甲 / 乙**：定分類表，還是「每張任務都要說出驗收判準」 | 兩條路的失敗模式不同，取捨看你要防哪一種 |
| 3 | **P0-2 的真閘門要不要現在做？** 還是先誠實降級、另立一件 | 影響這個 change 的大小 |
| 4 | **合併還要不要維持？** 審查後這件事比原估大得多 | 8/26 曾決定拆開，今天決定合併；資訊變了，值得重問一次 |
| 5 | **全域 `CLAUDE.md` 的複審紀律**要改成 `Codex → Fable`，還是本 repo 標例外 | 那是你的規矩 |

---

## 五、本次的紀律觀察

同一個病今天出現**三次**，載體各不相同：

1. **當初的作者**把上游的 `should use` 讀成 `internally enforces`
2. **8/26** 修了 Compatibility 表一列，沒往外掃同類（半修）
3. **今天的我**：設計「怎麼要求證據」設計了一個下午，**卻沒去讀我們指定要用的那個 skill
   自己怎麼定義證據**——而它明文寫著 `never grep its text`

三次都是**讀了名字、沒讀它實際說什麼**。

第 3 次還附帶一個更難堪的細節：我在同一次對話裡**先引用了 `task-reviewer-prompt.md:75`
當反向證據**，半小時後又寫下「所有 TDD 字樣都在 implementer-prompt」這句排除它的絕對句。
**反例當時就在我自己的輸出裡。**
